# Requirements for the planned research infrastructure.
# NOTE: versions are intentionally unpinned until the environment is finalized.
# Model-specific SDKs will be added only after the model list is confirmed.

pandas
numpy
scipy
scikit-learn
matplotlib
seaborn
tqdm
python-dotenv
requests