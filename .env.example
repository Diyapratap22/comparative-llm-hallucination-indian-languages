# ---------------------------------------------------------------------------
# Environment variables (placeholders only)
# ---------------------------------------------------------------------------
# Copy this file to `.env` and fill in values locally. NEVER commit `.env`
# (it is ignored by git). No real credentials exist in this repository.
#
# WARNING: the model list and API configuration are NOT finalized. These
# variable names are illustrative placeholders and will be confirmed when the
# experiment is configured.
# ---------------------------------------------------------------------------

OPENAI_API_KEY=
GOOGLE_API_KEY=